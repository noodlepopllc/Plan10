Your job is to expand the world scaffolding provided in the input text into a complete static world model.
You MUST treat the input as the ONLY source of truth.
You MUST output the following sections in this exact order:
⭐ SECTION 1 — SETTING (2–3 paragraphs)

Expand the World Summary found in the input into:

    atmosphere

    time period (MANDATORY — inferred from SETTING BLOCK)

    technology level (MANDATORY — inferred from SETTING BLOCK)

    sensory detail

    tone

    cultural context

    environmental mood

Everything MUST be cinematic and filmable, but NOT narrative.
You MUST anchor all descriptions to the time period and technology level defined by the SETTING BLOCK.
You MUST NOT add story events, character actions, or plot.
⭐ SECTION 2 — BIOGRAPHY (full detail)

For each character described in the input Character Summary, expand into:

    Name

    Age

    Gender

    Race (e.g., human, android, elf, etc.)

    Ethnicity/Species (only if explicitly given)

    Appearance (visual, physical, filmable)

    Clothing (MANDATORY — must match the time period + technology level of the SETTING BLOCK)

    Hair (MANDATORY — must match the time period + technology level)

    Personality (1–2 sentences)

    Movement style

    Distinctive physical traits

STRICT RULES:

    MUST be visual, physical, and filmable

    MUST reflect the time period and technology level of the SETTING BLOCK

    MUST NOT include backstory or lore

    MUST NOT include internal monologue

    MUST NOT include emotional analysis

    MUST NOT include story events

⭐ SECTION 3 — LOCATIONS (full detail)

For each location described in the input Location Summary, expand it into:
A. Exterior Description (MANDATORY)

2–3 sentences describing:

    architecture (consistent with time period + technology level)

    materials (consistent with time period + technology level)

    lighting (consistent with time period + technology level)

    signage (if applicable)

    atmosphere

    ambient sound

B. Interior STAGING ZONES (MANDATORY)

Zones are functional staging areas inside the location.

Rules:

    MUST derive zones ONLY from the Location Summary

    MUST define 3–8 meaningful zones

    MUST be filmable patches of space

    MUST match the time period + technology level

    MUST NOT invent new zones

For each zone:

    2–3 sentences of description

    3–5 visual details (architecture, materials, lighting, fixed furniture)

    NO story events

    NO props

    NO character details

C. Notable Details

REMOVE THIS SECTION ENTIRELY.  
Notable Details is deprecated and MUST NOT be generated.
⭐ GLOBAL RULES (PASS B)

    MUST use ONLY the provided input text as the source of truth

    MUST preserve the time period and technology level defined by the SETTING BLOCK

    MUST NOT invent new locations

    MUST NOT invent impossible architecture for the time period

    MUST NOT write any story or narrative

    MUST NOT include camera terminology

    MUST NOT skip any required section

