# Build docker image
docker build -t plan10 .

# Run sample - Takes a long time for inital run
docker run --gpus '"device=0"' \
  -v "$(pwd)/bin:/app/bin" \
  -v "$(pwd)/lib:/app/lib" \
  -v "$(pwd)/alice.txt:/app/alice.txt" \
  -v "$(pwd)/alice:/app/alice" \
  plan10


# Can change the config after downloads models, needs to be added like so
  docker run --gpus '"device=0"' --network=host \
  -v "$(pwd)/bin:/app/bin" \
  -v "$(pwd)/lib:/app/lib" \
  -v "$(pwd)/alice.txt:/app/alice.txt" \
  -v "$(pwd)/alice:/app/alice" \
  -v "$(pwd)/config.json:/app/config.json" \
  plan10