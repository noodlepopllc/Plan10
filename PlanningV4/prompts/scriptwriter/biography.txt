⭐ UPDATED PASS 0B — FILMABLE SET DEFINITION (ROOM‑LEVEL)

(With Purpose added to Zones, without violating architectural determinism)
⭐ SECTION 1 — SETTING

(unchanged — but clarified)

Goal: Expand the SETTING BLOCK from PASS 0A into a single filmable room that is:

    open, breathable, and spacious

    consistent with time period + technology level

    grounded in sensory detail

    architecturally coherent

    large enough to support multiple orientations

Rules:

    MUST describe the room as open, wide, or expansive unless PASS 0A forbids it.

    MUST anchor all descriptions to time period + technology level.

    MUST NOT add story events, plot, or character actions.

    MUST NOT invent new technology.

    MUST NOT imply other rooms unless PASS 0A explicitly mentions them.

⭐ SECTION 2 — BIOGRAPHY

(unchanged — but clarified)

For each character, output:

    Name

    Age

    Gender

    Race

    Ethnicity/Species (only if explicitly given)

    Appearance (visual, physical, filmable)

    Clothing (time‑period appropriate)

    Hair (time‑period appropriate)

    Personality (1–2 filmable traits)

    Movement style (compatible with open‑space blocking)

    Distinctive physical traits

Rules:

    MUST NOT include backstory, internal monologue, or narrative events.

    MUST NOT contradict the room’s spaciousness.

⭐ SECTION 3 — LOCATIONS (ROOM‑LEVEL)

PASS 0B outputs one LOCATION = one room.
⭐ A. Architectural Shell

(unchanged — but clarified)

3–5 sentences describing ONLY:

    the room’s overall form

    major fixed structures explicitly mentioned in PASS 0A

    openings explicitly mentioned in PASS 0A

    ground/terrain material

    lighting sources

    architectural style

    spatial scale (MUST feel open unless input forbids it)

Rules:

    MUST bias toward open floor plans, wide volumes, generous negative space.

    MUST NOT invent geometry or structures not implied by PASS 0A.

    MUST NOT invent props or furniture.

⭐ B. Environment Objects

List anchored, non‑movable objects explicitly mentioned in PASS 0A:

    material

    color

    placement

    orientation

    attachment to fixed structure (if applicable)

Rules:

    MUST describe placement with ample spacing.

    MUST NOT invent new objects.

⭐ C. ZONES (ANGLES INSIDE THE ROOM)

Each Zone now contains THREE subsections:

    Zone Name

    Zone Definition (purely physical)

    Purpose (NEW — semantic, but non‑architectural)

    Anchored Elements

This preserves determinism while giving the narrator semantic richness.
⭐ Zone Name

A short label for the functional sub‑area.
⭐ 1. Zone Definition (Physical Only)

3–5 sentences describing:

    what part of the room the zone represents

    which fixed environmental features define it

    how it relates to the larger room

    the architectural function (seating cluster, entry threshold, counter area, etc.)

    the general spatial feel (open, breathable)

    the presence of two usable orientations (without mentioning cameras)

Rules:

    MUST be purely physical.

    MUST NOT reference people, actions, or narrative behavior.

    MUST imply two opposite, filmable sides.

    MUST describe anchored elements defining one side and open space on the opposite side.

    MUST support clean reverse‑shot staging.

Allowed phrasing:

    “The anchored elements define one side of the zone, leaving the opposite side open.”

    “The space maintains clear sightlines in both directions.”

    “The layout naturally supports viewing the zone from multiple orientations.”

    “The zone remains wide and breathable, with open space extending behind the primary features.”

⭐ 2. Purpose (Narrative‑Friendly, Non‑Architectural)

(NEW — added per your request)

1–2 sentences describing:

    the functional or narrative purpose of the zone

    how the zone is typically used in the world (e.g., dining, ordering, entering)

    WITHOUT describing actions happening right now

    WITHOUT referencing specific characters

    WITHOUT adding story events

Examples:

    “A natural area for seated dining and quiet moments.”

    “A transitional space used for entering or exiting the room.”

    “A functional zone for ordering or customer interaction.”

Rules:

    MUST NOT affect geometry.

    MUST NOT imply current activity.

    MUST NOT introduce new objects.

    MUST NOT reference cameras.

⭐ 3. Anchored Elements

List 3–6 fixed or semi‑fixed objects explicitly mentioned in PASS 0A:

    what they are

    what material they are

    where they are positioned

    how they are oriented

Rules:

    MUST describe placement with clear spacing.

    MUST position anchored elements so they define one side of the zone.

    MUST leave the opposite side open.

    MUST NOT invent new objects.

    MUST NOT add story.

⭐ GLOBAL RULES (SET DEFINITION LAYER)

    MUST use ONLY the input text as truth.

    MUST NOT invent new locations, furniture, or architecture.

    MUST NOT add narrative or character actions.

    MUST NOT skip any required section.

    MUST bias toward spacious, open, two‑orientation‑compatible environments unless the input forbids it.

This layer’s ONLY job:  
Turn conceptual scaffolding into a real, anchored, spacious, two‑view‑compatible room‑level film set.